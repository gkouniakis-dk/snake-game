name: Build Executables

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            artifact_name: snake-game
            asset_name: snake-game-linux
          - os: windows-latest
            artifact_name: snake-game.exe
            asset_name: snake-game-windows
          - os: macos-latest
            artifact_name: snake-game
            asset_name: snake-game-macos

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build with PyInstaller
      run: pyinstaller snake_game.spec

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.asset_name }}
        path: dist/

  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: release-files

    - name: List artifacts
      run: ls -R release-files/

    - name: Prepare files for release
      run: |
        mkdir -p release-assets
        cp release-files/snake-game-windows/* release-assets/ 2>/dev/null || true
        cp release-files/snake-game-macos/* release-assets/ 2>/dev/null || true
        cp release-files/snake-game-linux/* release-assets/ 2>/dev/null || true
        ls -la release-assets/

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: release-assets/*
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
