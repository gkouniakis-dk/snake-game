name: Build Executables

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    - name: Build with PyInstaller
      run: pyinstaller snake_game.spec
    - name: Upload Windows artifact
      uses: actions/upload-artifact@v4
      with:
        name: windows-exe
        path: dist/snake-game.exe

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'
    - name: Install system dependencies
      run: |
        brew install sdl2 sdl2_image sdl2_mixer sdl2_ttf
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    - name: Build with PyInstaller
      run: pyinstaller snake_game.spec
    - name: Zip macOS app
      run: cd dist && zip -r snake-game-macos.zip snake-game.app && ls -lah *.zip
    - name: Upload macOS artifact
      uses: actions/upload-artifact@v4
      with:
        name: macos-app
        path: dist/snake-game-macos.zip

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    - name: Build with PyInstaller
      run: pyinstaller snake_game.spec
    - name: Upload Linux artifact
      uses: actions/upload-artifact@v4
      with:
        name: linux-exe
        path: dist/snake-game

  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-windows, build-macos, build-linux]
    if: startsWith(github.ref, 'refs/tags/')
    steps:
    - uses: actions/checkout@v4
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts
    - name: Prepare release files
      run: |
        echo "=== All artifacts ==="
        find ./artifacts -type f
        echo ""
        echo "=== Flattening artifacts ==="
        mkdir -p ./release-assets
        find ./artifacts -type f -exec cp {} ./release-assets/ \;
        echo ""
        echo "=== Final release assets ==="
        ls -lah ./release-assets/
    - name: Delete existing release if it exists
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh release delete "${{ github.ref_name }}" --yes || true
    - name: Create Release with GitHub CLI
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "=== Files in release-assets ==="
        ls -lah ./release-assets/
        gh release create "${{ github.ref_name }}" \
          --title "Release ${{ github.ref_name }}" \
          --generate-notes \
          ./release-assets/snake-game.exe \
          ./release-assets/snake-game-macos.zip \
          ./release-assets/snake-game
